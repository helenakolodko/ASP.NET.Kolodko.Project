@{
    ViewBag.Title = "Index";
}

<div class="jumbotron">
    <div class="container">
        <h1>Forum</h1>
        <p>Just talk out.</p>
        <a href="#">Learn More</a>
    </div>
</div>

<div class="container content" >

</div>

@*@Html.Action("GetLatestTopics") [ChildActionOnly] return PartialView("_TopicsTable", topics)*@

@*@Html.Action("GetComments", topic) [ChildActionOnly] *@

@*partial views for topics table and comments list*@

@*resize:both;overflow:auto;
    
    :link :visited :hover :active - only in this order
    
    margin-top and margin-bottom are not  separated for block elements
    parent and child borders are touching if border is not specified
    
    margin: 0 auto; -- for centering things
    
    rasor always encode string values, so you need return MvcHtmlString. use TagBuilder to build return string.*@
